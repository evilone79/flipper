# One of its kind bit flipping client

### Installation

```
git clone https://github.com/evilone79/flipper

cd flipper

./build.sh
```

### Usage

Credentials setup:
```
echo 'username' >> creds
echo 'password' >> creds
```
Run `./install/bin/flipper`, lean back and relax
